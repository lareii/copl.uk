import LayoutContent from '@/app/app/settings/layout-content';

export const metadata = {
  title: 'ayarlar'
};

export default function Layout({ children }) {
  return <LayoutContent>{children}</LayoutContent>;
}
